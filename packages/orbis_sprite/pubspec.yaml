name: orbis_sprite
description: >-
  Two dimensions: atlases, sprite animation, parallax layers and tile maps.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.10.0

dependencies:
  vector_math: ^2.1.4

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
