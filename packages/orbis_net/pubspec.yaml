name: orbis_net
description: >-
  Multiplayer for Orbis. Replicates component columns rather than object
  properties, over a transport the engine does not own.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.10.0

dependencies:
  orbis_core:
    path: ../orbis_core

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
